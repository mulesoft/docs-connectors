= MuleSoft WebCrawler Connector 1.0
:lc-connector-name: mulesoft-webcrawler
:connector-name: MuleSoft WebCrawler

Anypoint Connector for MuleSoft WebCrawler (MuleSoft WebCrawler Connector) is a highly customizable web-crawling connector that enables you to crawl websites, retrieve content, and automate web scraping processes within Mule flows. The connector is suited for gathering large-scale web data efficiently, with helper operations for fine-tuned crawling such as targeting specific content types, downloading documents, and downloading images. Use it to organize web data into structured formats for knowledge extraction, AI applications, and downstream processing.

For information about compatibility and fixed issues, see the MuleSoft WebCrawler Connector release notes.

== Before You Begin

To use this connector, you must be familiar with:

* Anypoint Connectors
* Mule runtime engine (Mule)
* Elements and global elements in a Mule flow
* How to create a Mule app using Anypoint Code Builder or Anypoint Studio
* Web crawling concepts and robots.txt compliance

Before creating an app, you must have:

* Java 11 or 17 (required for compilation and runtime)
* Apache Maven
* Anypoint Platform
* The latest versions of Anypoint Code Builder or Anypoint Studio

== Connection Types

MuleSoft WebCrawler Connector supports these connection types for different crawling scenarios.

* *HTTP*
+
Connects via standard HTTP requests with configurable user agent, referrer, response timeout, and TLS settings. Use for static web content that doesn't require JavaScript rendering.
* *Remote WebDriver*
+
Connects to a remote Selenium WebDriver endpoint (for example, Selenium Grid, standalone ChromeDriver, BrowserStack, or a CloudHub-hosted remote-browser app). Supports Chromium-only browsers (Chrome, Chromium, Edge). Use for dynamic web content rendered by JavaScript, single-page applications, or pages with Shadow DOM.

== Key Features

MuleSoft WebCrawler Connector simplifies web data extraction into Mule applications with:

* Comprehensive Web Crawling
+
Crawl entire websites or target specific content for large-scale data extraction with configurable depth and URL filtering.
* Customizable Crawls
+
Control crawl depth, content filters, URL inclusion/exclusion patterns, and crawl delays tailored to specific business needs.
* Content Extraction
+
Retrieve page content in TEXT, HTML, or MARKDOWN format. Target specific HTML tags to extract only relevant content.
* Document and Image Downloads
+
Download documents and images from web pages or via direct URLs, with configurable limits.
* Page Insights
+
Get analytical data about web pages including word count, element/tag counts, and link structures to help customize crawls.
* Dynamic Content Support
+
Crawl JavaScript-rendered pages using the Remote WebDriver connection, including Shadow DOM extraction and XPath-based wait conditions.
* Robots.txt Compliance
+
Optionally enforce robots.txt rules to ensure respectful crawling practices.
* Event-Driven Crawling
+
Use the On Site Crawl source to emit each discovered page as a Mule event, with deduplication and URL queue management.

== Use Cases

MuleSoft WebCrawler Connector supports a wide range of data ingestion and automation scenarios:

[%header%autowidth.spread]
|===
| Role | Goal
| Agentic Automation | Scrape internal documents to ground agents and bots with up-to-date knowledge.
| Data Analyst | Extract product information from e-commerce websites for competitive analysis.
| Marketing Team | Gather customer reviews from various platforms for sentiment tracking.
| Business Development | Collect contact information from industry directories for lead generation.
|===

=== Crawl Use Cases

* Full Website Ingestion
+
Perform a full crawl of a website, traversing links up to a defined depth. Extract page content in HTML, TEXT, or MARKDOWN format alongside a JSON manifest of discovered URLs and downloaded files.
* Streaming Crawl
+
A streaming variation of the full crawl that progressively emits page content as it is discovered, enabling real-time downstream processing without waiting for the entire crawl to complete.
* Sitemap Generation
+
Quickly enumerate all URLs of a target website, outputting a standards-compliant XML sitemap for SEO analysis or as input to subsequent targeted crawls.

=== Page Use Cases

* Content Extraction
+
Fetch the full rendered content of a single web page in your choice of format (HTML, TEXT, or MARKDOWN).
* Targeted Extraction
+
Extract only the parts of a page you need by specifying HTML tags in the Tag list parameter. For JavaScript-rendered pages, use XPath-based wait conditions through the Remote WebDriver connection.
* Dynamic Content Rendering
+
Load JavaScript-driven pages via the Remote WebDriver connection, capturing the fully rendered content instead of raw HTML. Ideal for single-page applications and pages that rely on client-side rendering.
* Media and Document Downloads
+
Retrieve and download images, PDFs, or other linked files from a page with configurable limits.

=== Authentication

MuleSoft WebCrawler Connector supports unauthenticated (public) websites. Authentication customization (for example, adding headers, cookies, or OAuth flows) can be achieved through partner extensions or external tooling.

== Next Step

After you complete the prerequisites, you are ready to create an app and configure the connector using xref:{lc-connector-name}-connector-studio.adoc[Anypoint Studio] or xref:{lc-connector-name}-connector-config-acb.adoc[Anypoint Code Builder].

== See Also

* xref:connectors::introduction/introduction-to-anypoint-connectors.adoc[Introduction to Anypoint Connectors]
* xref:{lc-connector-name}-connector-reference.adoc[MuleSoft WebCrawler Connector Reference]
* https://help.salesforce.com[Salesforce Help]
