
= MuleSoft Inference Connector 1.1 Reference

MuleSoft Inference Connector provides operations to interface directly with the API of various inference providers, enabling seamless integration of AI capabilities into your Mule applications.

== Configurations
---
[[ImageGenerationConfig]]
=== Image Generation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<ImageGenerationConfig_HerokuImage, Heroku AI>> 
* <<ImageGenerationConfig_HuggingFaceImage, Hugging Face>> 
* <<ImageGenerationConfig_OpenaiImage, OpenAI>> 
* <<ImageGenerationConfig_StabilityAiImage, Stability AI>> 
* <<ImageGenerationConfig_XaiImage, xAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types

[[ImageGenerationConfig_HerokuImage]]
===== Heroku AI

====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Heroku Inference Model a| String | Model name. |  | x
| Heroku Diffusion URL a| String | Heroku diffusion URL. |  `\https://us.inference.heroku.com` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===


[[ImageGenerationConfig_HuggingFaceImage]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_OpenaiImage]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_StabilityAiImage]]
===== Stability AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Stability AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_XaiImage]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<GenerateImage>> 


---
[[ModerationConfig]]
=== Moderation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<ModerationConfig_MistralaiModeration, Mistral AI>> 
* <<ModerationConfig_OpenaiModeration, OpenAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[ModerationConfig_MistralaiModeration]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ModerationConfig_OpenaiModeration]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<ToxicityDetectionText>> 


---
[[TextGenerationConfig]]
=== Text Generation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<TextGenerationConfig_Ai21labs, AI21Labs>> 
* <<TextGenerationConfig_Anthropic, Anthropic>> 
* <<TextGenerationConfig_AzureAiFoundry, Azure AI Foundry>> 
* <<TextGenerationConfig_AzureOpenai, Azure OpenAI>> 
* <<TextGenerationConfig_Cerebras, Cerebras>> 
* <<TextGenerationConfig_Cohere, Cohere>> 
* <<TextGenerationConfig_Databricks, Databricks>> 
* <<TextGenerationConfig_Deepinfra, DeepInfra>> 
* <<TextGenerationConfig_Deepseek, Deepseek>> 
* <<TextGenerationConfig_Docker, Docker>> 
* <<TextGenerationConfig_Fireworks, Fireworks>> 
* <<TextGenerationConfig_Gemini, Gemini>>
* <<TextGenerationConfig_Github, GitHub>> 
* <<TextGenerationConfig_Gpt4all, GPT4All>> 
* <<TextGenerationConfig_Groq, Groq>> 
* <<TextGenerationConfig_Heroku, Heroku AI>>
* <<TextGenerationConfig_HuggingFace, Hugging Face>> 
* <<TextGenerationConfig_Lmstudio, LM Studio>> 
* <<TextGenerationConfig_Mistralai, Mistral AI>> 
* <<TextGenerationConfig_Nvidia, Nvidia>> 
* <<TextGenerationConfig_Ollama, Ollama>> 
* <<TextGenerationConfig_Openai, OpenAI>> 
* <<TextGenerationConfig_OpenaiCompatible, OpenAI Compatible Endpoint>> 
* <<TextGenerationConfig_Openrouter, OpenRouter>> 
* <<TextGenerationConfig_Perplexity, Perplexity>> 
* <<TextGenerationConfig_Portkey, Portkey>> 
* <<TextGenerationConfig_Together, Together>> 
* <<TextGenerationConfig_VertexaiExpress, Vertex AI Express>> 
* <<TextGenerationConfig_Xai, xAI>> 
* <<TextGenerationConfig_Xinference, XInference>> 
* <<TextGenerationConfig_ZhipuAi, ZHIPU_AI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[TextGenerationConfig_Ai21labs]]
===== AI21Labs


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ai21 Labs Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Anthropic]]
===== Anthropic


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Anthropic Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_AzureAiFoundry]]
===== Azure AI Foundry


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure Model Name a| String | Model name. |  | x
| [Azure AI Foundry] Resource Name a| String | Azure AI Foundry resource name. |  | 
| [Azure AI Foundry] API Version a| String | Azure AI Foundry API version. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_AzureOpenai]]
===== Azure OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure Model Name a| String | Model name. |  | x
| [Azure OpenAI] API Version a| String | Azure OpenAI API version. |  2024-10-21 |
| [Azure OpenAI] User a| String |  Unique identifier representing your end-user, which can help to monitor and detect abuse. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| [Azure OpenAI] Endpoint a| String |  The complete endpoint for the Azure OpenAI service. You can either provide the default or a custom endpoint URL (which is used directly and overrides any parameter values), or provide the resource name and deployment ID (which are used to construct the base URL). |  |x
| [Azure OpenAI] Resource Name a| String |  The resource name of the Azure OpenAI service used to construct the endpoint. This parameter is used if you do not provide a custom endpoint URL. |  |
| [Azure OpenAI] Deployment ID a| String |  The Deployment ID of the Azure OpenAI service used to construct the endpoint. This parameter is used if you do not provide a custom endpoint URL. |  |
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Cerebras]]
===== Cerebras


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Cerebras Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Cohere]]
===== Cohere


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Cohere Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Databricks]]
===== Databricks


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| [Databricks] Serving Endpoint Name a| String | Databricks serving endpoint name. |  | x
| [Databricks] Serving Endpoint URL host a| String | Databricks serving endpoint URL host. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Deepinfra]]
===== DeepInfra


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Deep Infra Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Deepseek]]
===== Deepseek


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Deepseek Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Docker]]
===== Docker


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Docker Model Name a| String | Model name. |  | x
| [Docker Models] Base URL a| String | Docker models base URL. |  `\http://localhost:12434` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Fireworks]]
===== Fireworks


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Fireworks Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 |
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS |
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. | |
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Gemini]]
===== Gemini


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> |  Configures a proxy for outbound connections. |  | 
| Gemini Model Name a| String |  Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Github]]
===== GitHub


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Git Hub Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Gpt4all]]
===== GPT4All


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Gpt4all Model Name a| String | Model name. |  | x
| [GPT4ALL] Base URL a| String | GPT4ALL base URL. |  `\http://localhost:4891/v1` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Groq]]
===== Groq


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Groq Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===


[[TextGenerationConfig_Heroku]]
===== Heroku AI

====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Heroku Inference Model a| String | Model name. |  `claude-3-7-sonnet` | 
| Heroku Inference URL a| String | Inference URL. |  `\https://us.inference.heroku.com` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===


[[TextGenerationConfig_HuggingFace]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===


[[TextGenerationConfig_Lmstudio]]
===== LM Studio


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Lm Studio Model Name a| String | Model name. |  | x
| [LM Studio] Base URL a| String | LM Studio base URL. |  `\http://localhost:1234/v1` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Mistralai]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Nvidia]]
===== Nvidia


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Nvidia Model Name a| String |  Model Name. |  | x
| [Nvidia] Base URL a| String | Nvidia base URL. |  `\https://integrate.api.nvidia.com` |
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Ollama]]
===== Ollama


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ollama Model Name a| String | Model name. |  | x
| [Ollama] Base URL a| String | Ollama base URL. |  `\http://localhost:11434/api` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Openai]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | OpenAI model name. |  `gpt-4o-mini` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_OpenaiCompatible]]
===== OpenAI Compatible Endpoint


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Compatible Model Name a| String | OpenAI compatible model name. |  `gpt-4o-mini` | 
| [OpenAI Compatible] Base URL a| String | OpenAI compatible base URL. |  `\https://server.endpoint.com` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Openrouter]]
===== OpenRouter


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open Router Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Perplexity]]
===== Perplexity


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Perplexity Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Portkey]]
===== Portkey


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Portkey Model Name a| String | Model name. |  | x
| [Portkey] Virtual Key a| String | Portkey virtual key. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Together]]
===== Together


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Together Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

[[TextGenerationConfig_VertexaiExpress]]
===== Vertex AI Express


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Express Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Xai]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Xinference]]
===== XInference


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Inference Model Name a| String | Model name. |  | x
| [Xinference] Base URL a| String | Xinference base URL. |  `\https://inference.top/api/v1` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_ZhipuAi]]
===== ZHIPU_AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Zhipu AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<AgentDefinePromptTemplate>> 
* <<ChatAnswerPrompt>> 
* <<ChatCompletions>> 
* <<ToolsNativeTemplate>> 


---
[[VisionConfig]]
=== Vision Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<VisionConfig_AnthropicVision, Anthropic>> 
* <<VisionConfig_AzureAiFoundryVision, Azure AI Foundry>> 
* <<VisionConfig_GeminiVision, Gemini>>
* <<VisionConfig_GithubVision, GitHub>> 
* <<VisionConfig_GroqVision, Groq>> 
* <<VisionConfig_HuggingFaceVision, Hugging Face>> 
* <<VisionConfig_MistralaiVision, Mistral AI>> 
* <<VisionConfig_OllamaVision, Ollama>> 
* <<VisionConfig_OpenaiVision, OpenAI>> 
* <<VisionConfig_OpenrouterVision, OpenRouter>> 
* <<VisionConfig_PortkeyVision, Portkey>> 
* <<VisionConfig_VertexaiExpressVision, Vertex AI Express>> 
* <<VisionConfig_XaiVision, xAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[VisionConfig_AnthropicVision]]
===== Anthropic


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Anthropic Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_AzureAiFoundryVision]]
===== Azure AI Foundry


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure AI Foundry Model Name a| String | Model name. |  | x
| [Azure AI Foundry] Resource Name a| String | Azure AI Foundry resource name. |  | 
| [Azure AI Foundry] API Version a| String | Azure AI Foundry API version. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_GeminiVision]]
===== Gemini


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> |  Configures a proxy for outbound connections. |  | 
| Gemini Model Name a| String |  Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number | Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_GithubVision]]
===== GitHub


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Git Hub Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_GroqVision]]
===== Groq


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Groq Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_HuggingFaceVision]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_MistralaiVision]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OllamaVision]]
===== Ollama


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ollama Model Name a| String | Model name. |  | x
| [Ollama] Base URL a| String | Ollama base URL. |  `\http://localhost:11434/api` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OpenaiVision]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OpenrouterVision]]
===== OpenRouter


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open Router Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_PortkeyVision]]
===== Portkey


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Portkey Model Name a| String | Model name. |  | x
| [Portkey] Virtual Key a| String | Portkey virtual key. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_VertexaiExpressVision]]
===== Vertex AI Express


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Express Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

[[VisionConfig_XaiVision]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Custom headers a| Array of <<DefaultHeader>> |  Custom headers are additional HTTP headers to include with each request. For any existing header key, the custom value will take precedence and overwrite the existing one.|  |V
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Controls diversity by creating a nucleus of the most probable words to choose from for the next token. Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<ReadImage>> 



== Operations

[[GenerateImage]]
== [Image] Generate (only Base64)
`<ms-inference:generate-image>`

This operation generates an image based on a prompt.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Prompt a| String |  User's prompt. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<ImageResponseAttributes>>
|===

=== For Configurations

* <<ImageGenerationConfig>> 

=== Throws

* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:IMAGE_GENERATION_FAILURE 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 


[[ToxicityDetectionText]]
== [Toxicity] Detection by Text
`<ms-inference:toxicity-detection-text>`

This operation detects harmful content in a text.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Text a| Any |  Text to moderate. Can be a single string or an array of strings. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
|===

=== For Configurations

* <<ModerationConfig>> 

=== Throws

* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 
* MS-INFERENCE:TOXICITY_DETECTION_OPERATION_FAILURE 


[[AgentDefinePromptTemplate]]
== [Agent] Define Prompt Template
`<ms-inference:agent-define-prompt-template>`


This operation defines a prompt template with instructions and data.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Template a| String |  Template string. |  | x
| Instructions a| String |  Instructions for the LLM. |  | x
| Data a| String |  Primary data content. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MS-INFERENCE:CHAT_OPERATION_FAILURE 
* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:INVALID_PROVIDER 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 
* MS-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ChatAnswerPrompt]]
== [Chat] Answer Prompt
`<ms-inference:chat-answer-prompt>`


This operation provides a simple chat answer for a single prompt.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Prompt a| String |  User's prompt. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MS-INFERENCE:CHAT_OPERATION_FAILURE 
* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:INVALID_PROVIDER 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 
* MS-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ChatCompletions]]
== [Chat] Completions
`<ms-inference:chat-completions>`


This operation provides chat completions by a messages array, including system or user messages (conversation history).  


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Messages a| Array of Any |  Conversation history as a JSON array. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MS-INFERENCE:CHAT_OPERATION_FAILURE 
* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:INVALID_PROVIDER 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 
* MS-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ToolsNativeTemplate]]
== [Tools] Native Template (Reasoning only)
`<ms-inference:tools-native-template>`


This operation defines a tools template with instructions and data. This operation is useful if you want to create autonomous agents that can use external tools whenever a prompt can't be answered directly by the AI model. This operation selects which tools can be used for execution.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Template a| String |  Template string. |  | x
| Instructions a| String |  Instructions for the LLM. |  | x
| Data a| String |  Primary data content. |  `#[payload]` | 
| Tools a| Array of Any |  Tools configuration as a JSON array. |  | x
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MS-INFERENCE:CHAT_OPERATION_FAILURE 
* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:INVALID_PROVIDER 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:RETRY_EXHAUSTED 
* MS-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ReadImage]]
== [Image] Read by (Url or Base64)
`<ms-inference:read-image>`


This operation reads an image from a URL or Base64 string.


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | Name of the configuration to use. | | x
| Prompt a| String |  User's prompt. |  | x
| Image a| String |  Image URL or Base64 string to send to the Vision Model. |  `#[payload]` | 
| Output Mime Type a| String |  MIME type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  Encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  Name of the configuration to use to execute this component. |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that stores the operation's output. |  | 
| Target Value a| String |  Expression that evaluates the operation’s output. The outcome of the expression is stored in the *Target Variable* field. |  `#[payload]` | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  Retry strategy in case of connectivity errors. |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<VisionConfig>> 

=== Throws

* MS-INFERENCE:CONNECTIVITY 
* MS-INFERENCE:INVALID_CONNECTION 
* MS-INFERENCE:INVALID_PROVIDER 
* MS-INFERENCE:RATE_LIMIT_EXCEEDED 
* MS-INFERENCE:READ_IMAGE_OPERATION_FAILURE 
* MS-INFERENCE:RETRY_EXHAUSTED 



== Types

[[Tls]]
=== TLS

Configures TLS to provide secure communications for the Mule app.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Enabled Protocols a| String | Comma-separated list of protocols enabled for this context. |  |
| Enabled Cipher Suites a| String | Comma-separated list of cipher suites enabled for this context. |  |
| Trust Store a| <<TrustStore>> | Configures the TLS truststore. |  |
| Key Store a| <<KeyStore>> | Configures the TLS keystore. |  |
| Revocation Check a| * <<StandardRevocationCheck>>
* <<CustomOcspResponder>>
* <<CrlFile>> | Configures a revocation checking mechanism. |  |
|===


[[TrustStore]]
=== Truststore

Configures the truststore for TLS.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | Path to the truststore. Mule resolves the path relative to the current classpath and file system. |  |
| Password a| String | Password used to protect the truststore. |  |
| Type a| String | Type of truststore. |  |
| Algorithm a| String | Encryption algorithm that the truststore uses. |  |
| Insecure a| Boolean | If `true`, Mule stops performing certificate validations. Setting this to `true` can make connections vulnerable to attacks. |  |
|===

[[KeyStore]]
=== Keystore

Configures the keystore for the TLS protocol. The keystore you generate contains a private key and a public certificate.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | Path to the keystore. Mule resolves the path relative to the current classpath and file system. |  |
| Type a| String | Type of keystore. |  |
| Alias a| String | Alias of the key to use when the keystore contains multiple private keys. By default, Mule uses the first key in the file. |  |
| Key Password a| String | Password used to protect the private key. |  |
| Password a| String | Password used to protect the keystore. |  |
| Algorithm a| String | Encryption algorithm that the keystore uses. |  |
|===

[[StandardRevocationCheck]]
=== Standard Revocation Check

Configures standard revocation checks for TLS certificates.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Only End Entities a| Boolean a| Which elements to verify in the certificate chain:

* `true`
+
Verify only the last element in the certificate chain.

* `false`
+
Verify all elements in the certificate chain. |  |
| Prefer Crls a| Boolean a| How to check certificate validity:

* `true`
+
Check the Certification Revocation List (CRL) for certificate validity.

* `false`
+
Use the Online Certificate Status Protocol (OCSP) to check certificate validity. |  |
| No Fallback a| Boolean a| Whether to use the secondary method to check certificate validity:

* `true`
+
Use the method that wasn't specified in the *Prefer Crls* field (the secondary method) to check certificate validity.

* `false`
+
Do not use the secondary method to check certificate validity. |  |
| Soft Fail a| Boolean a| What to do if the revocation server can't be reached or is busy:

* `true`
+
Avoid verification failure.

* `false`
+
Allow the verification to fail. |  |
|===

[[CustomOcspResponder]]
=== Custom OCSP Responder

Configures a custom OCSP responder for certification revocation checks.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Url a| String | URL of the OCSP responder. |  |
| Cert Alias a| String | Alias of the signing certificate for the OCSP response. If specified, the alias must be in the truststore. |  |
|===


[[CrlFile]]
=== CRL File

Specifies the location of the certification revocation list (CRL) file.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | Path to the CRL file. |  |
|===

[[DefaultHeader]]
=== Header

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Key a| String | Header name. | | x
| Value a| String | Header value. | | x
|===

[[Reconnection]]
=== Reconnection

Configures a reconnection strategy for an operation.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Fails Deployment a| Boolean | Configures a reconnection strategy to use when a connector operation fails to connect to an external server. |  |
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> | Reconnection strategy to use. |  |
|===

[[Reconnect]]
=== Reconnect

Configures a standard reconnection strategy, which specifies how often to reconnect and how many reconnection attempts the connector source or operation can make.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Frequency a| Number | How often to attempt to reconnect, in milliseconds. |  |
| Blocking a| Boolean | If `false`, the reconnection strategy runs in a separate, non-blocking thread. |  |
| Count a| Number | How many reconnection attempts the Mule app can make. |  |
|===

[[ReconnectForever]]
=== Reconnect Forever

Configures a forever reconnection strategy by which the connector source or operation attempts to reconnect at a specified frequency for as long as the Mule app runs.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Frequency a| Number | How often to attempt to reconnect, in milliseconds. |  |
| Blocking a| Boolean | If `false`, the reconnection strategy runs in a separate, non-blocking thread. |  |
|===

[[ExpirationPolicy]]
=== Expiration Policy

Configures an expiration policy strategy.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Max Idle Time a| Number | Configures the maximum amount of time that a dynamic configuration instance can remain idle before Mule considers it eligible for expiration. |  |
| Time Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS | Time unit for the *Max Idle Time* field. |  |
|===

[[ImageResponseAttributes]]
=== Image Response Attributes

Configures image response attributes.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Model a| String | Model. |  | 
| Prompt Used a| String | Prompt used. |  | 
|===

[[RepeatableInMemoryStream]]
=== Repeatable In Memory Stream

Configures the in-memory streaming strategy by which the request fails if the data exceeds the MAX buffer size. Always run performance tests to find the optimal buffer size for your specific use case.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Initial Buffer Size a| Number | Initial amount of memory to allocate to the data stream. If the streamed data exceeds this value, the buffer expands by *Buffer Size Increment*, with an upper limit of *Max Buffer Size*. |  |
| Buffer Size Increment a| Number | This is by how much the buffer size expands if it exceeds its initial size. Setting a value of zero or lower means that the buffer should not expand, meaning that a `STREAM_MAXIMUM_SIZE_EXCEEDED` error is raised when the buffer gets full. |  |
| Max Buffer Size a| Number | Maximum size of the buffer. If the buffer size exceeds this value, Mule raises a `STREAM_MAXIMUM_SIZE_EXCEEDED` error. A value of less than or equal to `0` means no limit. |  |
| Buffer Unit a| Enumeration, one of:

** BYTE
** KB
** MB
** GB | Unit for the *Initial Buffer Size*, *Buffer Size Increment*, and *Max Buffer Size* fields. |  |
|===

[[RepeatableFileStoreStream]]
=== Repeatable File Store Stream

Configures the repeatable file-store streaming strategy by which Mule keeps a portion of the stream content in memory. If the stream content is larger than the configured buffer size, Mule backs up the buffer's content to disk and then clears the memory.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| In Memory Size a| Number a| Maximum amount of memory that the stream can use for data. If the amount of memory exceeds this value, Mule buffers the content to disk. To optimize performance:

* Configure a larger buffer size to avoid the number of times Mule needs to write the buffer on disk. This increases performance, but it also limits the number of concurrent requests your application can process, because it requires additional memory.

* Configure a smaller buffer size to decrease memory load at the expense of response time. |  |
| Buffer Unit a| Enumeration, one of:

** BYTE
** KB
** MB
** GB | Unit for the *In Memory Size* field. |  |
|===

[[ErrorMapping]]
=== Error Mapping

Configures error mapping. 

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Source a| Enumeration, one of:

** ANY
** REDELIVERY_EXHAUSTED
** TRANSFORMATION
** EXPRESSION
** SECURITY
** CLIENT_SECURITY
** SERVER_SECURITY
** ROUTING
** CONNECTIVITY
** RETRY_EXHAUSTED
** TIMEOUT | Source of the error. |  | 
| Target a| String | Target of the error. |  | x
|===

[[LlmResponseAttributes]]
=== LLM Response Attributes

Configures LLM response attributes.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Additional Attributes a| <<AdditionalAttributes>> | Additional attributes. |  | 
| Token Usage a| <<TokenUsage>> | Token usage. |  | 
|===

[[AdditionalAttributes]]
=== Additional Attributes

Configures additional attributes.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Content Filter Results a| Object | Flags if the generated response contains potentially harmful content. |  | 
| Finish Reason a| String | Finish reason for the LLM response. |  | 
| Id a| String | ID of the request. |  | 
| Model a| String | ID of the model used. |  | 
| Prompt Filter Results a| Array of PromptFilterResult | Flags if the user's input contains potentially harmful content. |  | 
|===

[[TokenUsage]]
=== Token Usage

Configures token usage metadata returned as attributes.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Input Count a| Number | Number of tokens used to process the input. |  | 
| Output Count a| Number | Number of tokens used to generate the output. |  | 
| Total Count a| Number | Total number of tokens used for input and output. |  | 
|===

[[Proxy]]
=== Proxy

Configures a proxy for outbound connections.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Host a| String | Host in which the proxy requests are sent. |  | x
| Port a| Number | Port in which the proxy requests are sent. |  | x
| Username a| String | Username to authenticate against the proxy. |  |
| Password a| String | Password to authenticate against the proxy. |  |
| Non Proxy Hosts a| String | Non-proxy hosts. |  | 
|===

[[NtlmProxy]]
=== NTLM Proxy

Configures an NTLM proxy for outbound connections.

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Ntlm Domain a| String | NTLM domain. |  | x
| Host a| String | Host in which the proxy requests are sent. |  | x
| Port a| Number | Port in which the proxy requests are sent. |  | x
| Username a| String | Username to authenticate against the proxy. |  | 
| Password a| String | Password to authenticate against the proxy. |  | 
| Non Proxy Hosts a| String | Non-proxy hosts. |  | 
|===

== See Also

* xref:index.adoc[MuleSoft Inference Connector Overview]
* xref:connectors::introduction/introduction-to-anypoint-connectors.adoc[Introduction to Anypoint Connectors]
* https://help.salesforce.com[Salesforce Help]
