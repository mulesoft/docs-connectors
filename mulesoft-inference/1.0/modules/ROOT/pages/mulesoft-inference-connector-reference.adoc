
= MuleSoft Inference Connector 1.0 Reference

MuleSoft Inference Connector provides operations to interface directly with the API of various inference providers, enabling seamless integration of AI capabilities into your Mule applications.

== Configurations
---
[[ImageGenerationConfig]]
=== Image Generation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<ImageGenerationConfig_HuggingFaceImage, Hugging Face>> 
* <<ImageGenerationConfig_OpenaiImage, OpenAI>> 
* <<ImageGenerationConfig_StabilityAiImage, Stability AI>> 
* <<ImageGenerationConfig_XaiImage, xAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[ImageGenerationConfig_HuggingFaceImage]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_OpenaiImage]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_StabilityAiImage]]
===== Stability AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Stability AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ImageGenerationConfig_XaiImage]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<GenerateImage>> 


---
[[ModerationConfig]]
=== Moderation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<ModerationConfig_MistralaiModeration, Mistral AI>> 
* <<ModerationConfig_OpenaiModeration, OpenAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[ModerationConfig_MistralaiModeration]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[ModerationConfig_OpenaiModeration]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<ToxicityDetectionText>> 


---
[[TextGenerationConfig]]
=== Text Generation Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<TextGenerationConfig_Ai21labs, AI21Labs>> 
* <<TextGenerationConfig_Anthropic, Anthropic>> 
* <<TextGenerationConfig_AzureAiFoundry, Azure AI Foundry>> 
* <<TextGenerationConfig_AzureOpenai, Azure OpenAI>> 
* <<TextGenerationConfig_Cerebras, Cerebras>> 
* <<TextGenerationConfig_Cohere, Cohere>> 
* <<TextGenerationConfig_Databricks, Databricks>> 
* <<TextGenerationConfig_Deepinfra, DeepInfra>> 
* <<TextGenerationConfig_Deepseek, Deepseek>> 
* <<TextGenerationConfig_Docker, Docker>> 
* <<TextGenerationConfig_Fireworks, Fireworks>> 
* <<TextGenerationConfig_Github, GitHub>> 
* <<TextGenerationConfig_Gpt4all, GPT4All>> 
* <<TextGenerationConfig_Groq, Groq>> 
* <<TextGenerationConfig_HuggingFace, Hugging Face>> 
* <<TextGenerationConfig_Ibmwatson, IBM Watson>> 
* <<TextGenerationConfig_Llmapi, Llm API>> 
* <<TextGenerationConfig_Lmstudio, LM Studio>> 
* <<TextGenerationConfig_Mistralai, Mistral AI>> 
* <<TextGenerationConfig_Nvidia, Nvidia>> 
* <<TextGenerationConfig_Ollama, Ollama>> 
* <<TextGenerationConfig_Openai, OpenAI>> 
* <<TextGenerationConfig_OpenaiCompatible, OpenAI Compatible Endpoint>> 
* <<TextGenerationConfig_Openrouter, OpenRouter>> 
* <<TextGenerationConfig_Perplexity, Perplexity>> 
* <<TextGenerationConfig_Portkey, Portkey>> 
* <<TextGenerationConfig_Together, Together>> 
* <<TextGenerationConfig_Vertexai, Vertex AI>> 
* <<TextGenerationConfig_VertexaiExpress, Vertex AI Express>> 
* <<TextGenerationConfig_Xai, xAI>> 
* <<TextGenerationConfig_Xinference, XInference>> 
* <<TextGenerationConfig_ZhipuAi, ZHIPU_AI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[TextGenerationConfig_Ai21labs]]
===== AI21Labs


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ai21 Labs Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Anthropic]]
===== Anthropic


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Anthropic Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_AzureAiFoundry]]
===== Azure AI Foundry


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure Model Name a| String | Model name. |  | x
| [Azure AI Foundry] Resource Name a| String |  |  | 
| [Azure AI Foundry] API Version a| String |  |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_AzureOpenai]]
===== Azure OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure Model Name a| String | Model name. |  | x
| [Azure OpenAI] Resource Name a| String | Azure OpenAI resource name. |  | 
| [Azure OpenAI] Deployment ID a| String | Azure OpenAI deployment ID. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Cerebras]]
===== Cerebras


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Cerebras Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Cohere]]
===== Cohere


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Cohere Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Databricks]]
===== Databricks


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| [Databricks] Serving Endpoint Name a| String | Databricks serving endpoint name. |  | x
| [Databricks] Serving Endpoint URL host a| String | Databricks serving endpoint URL host. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Deepinfra]]
===== DeepInfra


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Deep Infra Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Deepseek]]
===== Deepseek


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Deepseek Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Docker]]
===== Docker


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Docker Model Name a| String | Model name. |  | x
| [Docker Models] Base URL a| String | Docker models base URL. |  `\http://localhost:12434` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Fireworks]]
===== Fireworks


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Fireworks Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Github]]
===== GitHub


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Git Hub Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Gpt4all]]
===== GPT4All


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Gpt4all Model Name a| String | Model name. |  | x
| [GPT4ALL] Base URL a| String | GPT4ALL base URL. |  `\http://localhost:4891/v1` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Groq]]
===== Groq


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Groq Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_HuggingFace]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Ibmwatson]]
===== IBM Watson


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ibm Watson Model Name a| String | Model name. |  | x
| [IBM Watson] API Version a| String | IBM Watson API version. |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Llmapi]]
===== Llm API


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Llm Api Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Lmstudio]]
===== LM Studio


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Lm Studio Model Name a| String | Model name. |  | x
| [LM Studio] Base URL a| String | LM Studio base URL. |  `\http://localhost:1234/v1` | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Mistralai]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Nvidia]]
===== Nvidia


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Nvidia Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Ollama]]
===== Ollama


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ollama Model Name a| String | Model name. |  | x
| [Ollama] Base URL a| String |  |  http://localhost:11434/api | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Openai]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String |  |  gpt-4o-mini | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_OpenaiCompatible]]
===== OpenAI Compatible Endpoint


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Compatible Model Name a| String |  |  gpt-4o-mini | 
| [OpenAI Compatible] Base URL a| String |  |  https://server.endpoint.com | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Openrouter]]
===== OpenRouter


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open Router Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Perplexity]]
===== Perplexity


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Perplexity Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Portkey]]
===== Portkey


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Portkey Model Name a| String | Model name. |  | x
| [Portkey] Virtual Key a| String |  |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Together]]
===== Together


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Together Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Vertexai]]
===== Vertex AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Model Name a| String | Model name. |  | x
| [VertexAI] Location Id a| String |  |  us-central1 | 
| [VertexAI] Project Id a| String |  |  | 
| [VertexAI] Service Account Key a| String |  |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_VertexaiExpress]]
===== Vertex AI Express


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Express Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Xai]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_Xinference]]
===== XInference


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Inference Model Name a| String | Model name. |  | x
| [Xinference] Base URL a| String |  |  https://inference.top/api/v1 | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[TextGenerationConfig_ZhipuAi]]
===== ZHIPU_AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Zhipu AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<AgentDefinePromptTemplate>> 
* <<ChatAnswerPrompt>> 
* <<ChatCompletions>> 
* <<McpToolsNativeTemplate>> 
* <<ToolsNativeTemplate>> 


---
[[VisionConfig]]
=== Vision Config


==== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
|Name | String | Name for this configuration. Connectors reference the configuration with this name. | | x
| Connection a| * <<VisionConfig_AnthropicVision, Anthropic>> 
* <<VisionConfig_AzureAiFoundryVision, Azure AI Foundry>> 
* <<VisionConfig_GithubVision, GitHub>> 
* <<VisionConfig_GroqVision, Groq>> 
* <<VisionConfig_HuggingFaceVision, Hugging Face>> 
* <<VisionConfig_MistralaiVision, Mistral AI>> 
* <<VisionConfig_OllamaVision, Ollama>> 
* <<VisionConfig_OpenaiVision, OpenAI>> 
* <<VisionConfig_OpenrouterVision, OpenRouter>> 
* <<VisionConfig_PortkeyVision, Portkey>> 
* <<VisionConfig_VertexaiExpressVision, Vertex AI Express>> 
* <<VisionConfig_VertexaiVision, Vertex AI>> 
* <<VisionConfig_XaiVision, xAI>> 
 | Connection types for this configuration. | | x
| Name a| String |  ID used to reference this configuration. |  | x
| Expiration Policy a| <<ExpirationPolicy>> |  Configures an expiration policy for the configuration. |  | 
|===

==== Connection Types
[[VisionConfig_AnthropicVision]]
===== Anthropic


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Anthropic Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_AzureAiFoundryVision]]
===== Azure AI Foundry


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Azure AI Foundry Model Name a| String | Model name. |  | x
| [Azure AI Foundry] Resource Name a| String |  |  | 
| [Azure AI Foundry] API Version a| String |  |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_GithubVision]]
===== GitHub


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Git Hub Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_GroqVision]]
===== Groq


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Groq Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_HuggingFaceVision]]
===== Hugging Face


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Hugging Face Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_MistralaiVision]]
===== Mistral AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Mistral AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OllamaVision]]
===== Ollama


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Ollama Model Name a| String | Model name. |  | x
| [Ollama] Base URL a| String |  |  http://localhost:11434/api | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OpenaiVision]]
===== OpenAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_OpenrouterVision]]
===== OpenRouter


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Open Router Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_PortkeyVision]]
===== Portkey


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Portkey Model Name a| String | Model name. |  | x
| [Portkey] Virtual Key a| String |  |  | 
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_VertexaiExpressVision]]
===== Vertex AI Express


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Express Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_VertexaiVision]]
===== Vertex AI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| Vertex AI Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===
[[VisionConfig_XaiVision]]
===== xAI


====== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Proxy Configuration a| One of:

* <<Proxy>>
* <<NtlmProxy>> | Configures a proxy for outbound connections. |  | 
| X Ai Model Name a| String | Model name. |  | x
| TLS Configuration a| <<Tls>> |  If HTTPS is configured as a protocol, then you must configure at least the keystore configuration. |  | 
| API Key a| String |  API key as required by the inference provider. |  | x
| Timeout a| Number |  Response timeout value set for each inference HTTP request. |  60 | 
| Timeout Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS |  Response timeout unit for the *Timeout* field. |  SECONDS | 
| Max Tokens a| Number |  Defines the number of LLM tokens to use when generating a response. This field helps control the usage and costs when engaging with LLMs. |  500 | 
| Temperature a| Number |  Number between 0 and 2. The temperature is used to control the randomness of the output. When you set it higher, you'll get more random outputs. When you set it lower (towards 0), the values are more deterministic. |  0.9 | 
| Top P a| Number |  Specifies the cumulative probability score threshold that the tokens must reach. |  0.9 | 
| MCP Server Urls (SSE over HTTP) a| Object |  MuleSoft Inference Connector provides an integrated MCP client. The MCP servers must support SSE over HTTP. |  | 
| Reconnection a| <<Reconnection>> |  When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
|===

== Supported Operations
* <<ReadImage>> 



== Operations

[[GenerateImage]]
== [Image] Generate (only Base64)
`<mac-inference:generate-image>`


Chat completions by messages array including system, users messages i.e. conversation history


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Prompt a| String |  the users prompt |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<ImageResponseAttributes>>
|===

=== For Configurations

* <<ImageGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:IMAGE_GENERATION_FAILURE 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 


[[ToxicityDetectionText]]
== [Toxicity] Detection by Text
`<mac-inference:toxicity-detection-text>`


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Text a| Any |  Text to moderate. Can be a single string or an array of strings |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
|===

=== For Configurations

* <<ModerationConfig>> 

=== Throws

* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOXICITY_DETECTION_OPERATION_FAILURE 


[[AgentDefinePromptTemplate]]
== [Agent] Define Prompt Template
`<mac-inference:agent-define-prompt-template>`


Define a prompt template with instructions and data


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Template a| String |  the template string |  | x
| Instructions a| String |  instructions for the LLM |  | x
| Data a| String |  the primary data content |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CHAT_OPERATION_FAILURE 
* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:MCP_TOOLS_OPERATION_FAILURE 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ChatAnswerPrompt]]
== [Chat] Answer Prompt
`<mac-inference:chat-answer-prompt>`


Simple chat answer for a single prompt


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Prompt a| String |  the user's prompt |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CHAT_OPERATION_FAILURE 
* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:MCP_TOOLS_OPERATION_FAILURE 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ChatCompletions]]
== [Chat] Completions
`<mac-inference:chat-completions>`


Chat completions by messages array including system, users messages i.e. conversation history


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Messages a| Any |  the conversation history as a JSON array |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CHAT_OPERATION_FAILURE 
* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:MCP_TOOLS_OPERATION_FAILURE 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOOLS_OPERATION_FAILURE 


[[McpToolsNativeTemplate]]
== [MCP] Tooling
`<mac-inference:mcp-tools-native-template>`


Define a tools template with instructions, data and tools


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Template a| String |  the template string |  | x
| Instructions a| String |  instructions for the LLM |  | x
| Data a| String |  the primary data content |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CHAT_OPERATION_FAILURE 
* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:MCP_TOOLS_OPERATION_FAILURE 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ToolsNativeTemplate]]
== [Tools] Native Template (Reasoning only)
`<mac-inference:tools-native-template>`


Define a tools template with instructions, data and tools


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Template a| String |  the template string |  | x
| Instructions a| String |  instructions for the LLM |  | x
| Data a| String |  the primary data content |  #[payload] | 
| Tools a| Any |  tools configuration as a JSON array |  | x
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<TextGenerationConfig>> 

=== Throws

* MAC-INFERENCE:CHAT_OPERATION_FAILURE 
* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:MCP_TOOLS_OPERATION_FAILURE 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:RETRY_EXHAUSTED 
* MAC-INFERENCE:TOOLS_OPERATION_FAILURE 


[[ReadImage]]
== [Image] Read by (Url or Base64)
`<mac-inference:read-image>`


Chat completions by messages array including system, users messages i.e. conversation history


=== Parameters

[%header%autowidth.spread]
|===
| Name | Type | Description | Default Value | Required
| Configuration | String | The name of the configuration to use. | | x
| Prompt a| String |  the users prompt |  | x
| Image a| String |  the image Url to be sent to the Vision Model |  #[payload] | 
| Output Mime Type a| String |  The mime type of the payload that this operation outputs. |  | 
| Output Encoding a| String |  The encoding of the payload that this operation outputs. |  | 
| Config Ref a| ConfigurationProvider |  The name of the configuration to use to execute this component |  | x
| Streaming Strategy a| * <<RepeatableInMemoryStream>>
* <<RepeatableFileStoreStream>>
* non-repeatable-stream |  Configures how Mule processes streams. Repeatable streams are the default behavior. |  | 
| Target Variable a| String |  Name of the variable that storesoperation's output. |  | 
| Target Value a| String |  An expression to evaluate against the operation's output and store the expression outcome in the target variable |  #[payload] | 
| Error Mappings a| Array of <<ErrorMapping>> |  Set of error mappings |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> |  A retry strategy in case of connectivity errors |  | 
|===

=== Output

[%autowidth.spread]
|===
|Type |Any
| Attributes Type a| <<LlmResponseAttributes>>
|===

=== For Configurations

* <<VisionConfig>> 

=== Throws

* MAC-INFERENCE:CONNECTIVITY 
* MAC-INFERENCE:INVALID_CONNECTION 
* MAC-INFERENCE:INVALID_PROVIDER 
* MAC-INFERENCE:RATE_LIMIT_EXCEEDED 
* MAC-INFERENCE:READ_IMAGE_OPERATION_FAILURE 
* MAC-INFERENCE:RETRY_EXHAUSTED 



== Types
[[Tls]]
=== Tls

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Enabled Protocols a| String | A comma-separated list of protocols enabled for this context. |  | 
| Enabled Cipher Suites a| String | A comma-separated list of cipher suites enabled for this context. |  | 
| Trust Store a| <<TrustStore>> |  |  | 
| Key Store a| <<KeyStore>> |  |  | 
| Revocation Check a| * <<StandardRevocationCheck>>
* <<CustomOcspResponder>>
* <<CrlFile>> |  |  | 
|===

[[TrustStore]]
=== Trust Store

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | The location (which will be resolved relative to the current classpath and file system, if possible) of the trust store. |  | 
| Password a| String | The password used to protect the trust store. |  | 
| Type a| String | The type of store used. |  | 
| Algorithm a| String | The algorithm used by the trust store. |  | 
| Insecure a| Boolean | If true, no certificate validations will be performed, rendering connections vulnerable to attacks. Use at your own risk. |  | 
|===

[[KeyStore]]
=== Key Store

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | The location (which will be resolved relative to the current classpath and file system, if possible) of the key store. |  | 
| Type a| String | The type of store used. |  | 
| Alias a| String | When the key store contains many private keys, this attribute indicates the alias of the key that should be used. If not defined, the first key in the file will be used by default. |  | 
| Key Password a| String | The password used to protect the private key. |  | 
| Password a| String | The password used to protect the key store. |  | 
| Algorithm a| String | The algorithm used by the key store. |  | 
|===

[[StandardRevocationCheck]]
=== Standard Revocation Check

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Only End Entities a| Boolean | Only verify the last element of the certificate chain. |  | 
| Prefer Crls a| Boolean | Try CRL instead of OCSP first. |  | 
| No Fallback a| Boolean | Do not use the secondary checking method (the one not selected before). |  | 
| Soft Fail a| Boolean | Avoid verification failure when the revocation server can not be reached or is busy. |  | 
|===

[[CustomOcspResponder]]
=== Custom Ocsp Responder

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Url a| String | The URL of the OCSP responder. |  | 
| Cert Alias a| String | Alias of the signing certificate for the OCSP response (must be in the trust store), if present. |  | 
|===

[[CrlFile]]
=== Crl File

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Path a| String | The path to the CRL file. |  | 
|===

[[Reconnection]]
=== Reconnection

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Fails Deployment a| Boolean | When the application is deployed, a connectivity test is performed on all connectors. If set to `true`, deployment fails if the test doesn't pass after exhausting the associated reconnection strategy. |  | 
| Reconnection Strategy a| * <<Reconnect>>
* <<ReconnectForever>> | The reconnection strategy to use. |  | 
|===

[[Reconnect]]
=== Reconnect

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Frequency a| Number | How often in milliseconds to reconnect |  | 
| Blocking a| Boolean | If false, the reconnection strategy will run in a separate, non-blocking thread |  | 
| Count a| Number | How many reconnection attempts to make. |  | 
|===

[[ReconnectForever]]
=== Reconnect Forever

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Frequency a| Number | How often in milliseconds to reconnect |  | 
| Blocking a| Boolean | If false, the reconnection strategy will run in a separate, non-blocking thread |  | 
|===

[[ExpirationPolicy]]
=== Expiration Policy

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Max Idle Time a| Number | A scalar time value for the maximum amount of time a dynamic configuration instance should be allowed to be idle before it's considered eligible for expiration |  | 
| Time Unit a| Enumeration, one of:

** NANOSECONDS
** MICROSECONDS
** MILLISECONDS
** SECONDS
** MINUTES
** HOURS
** DAYS | A time unit that qualifies the maxIdleTime attribute |  | 
|===

[[ImageResponseAttributes]]
=== Image Response Attributes

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Model a| String |  |  | 
| Prompt Used a| String |  |  | 
|===

[[RepeatableInMemoryStream]]
=== Repeatable In Memory Stream

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Initial Buffer Size a| Number | The amount of memory that will be allocated to consume the stream and provide random access to it. If the stream contains more data than can be fit into this buffer, then the buffer expands according to the bufferSizeIncrement attribute, with an upper limit of maxInMemorySize. |  | 
| Buffer Size Increment a| Number | This is by how much the buffer size expands if it exceeds its initial size. Setting a value of zero or lower means that the buffer should not expand, meaning that a STREAM_MAXIMUM_SIZE_EXCEEDED error is raised when the buffer gets full. |  | 
| Max Buffer Size a| Number | The maximum amount of memory to use. If more than that is used then a STREAM_MAXIMUM_SIZE_EXCEEDED error is raised. A value lower than or equal to zero means no limit. |  | 
| Buffer Unit a| Enumeration, one of:

** BYTE
** KB
** MB
** GB | The unit in which all these attributes are expressed |  | 
|===

[[RepeatableFileStoreStream]]
=== Repeatable File Store Stream

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| In Memory Size a| Number | Defines the maximum memory that the stream should use to keep data in memory. If more than that is consumed content on the disk is buffered. |  | 
| Buffer Unit a| Enumeration, one of:

** BYTE
** KB
** MB
** GB | The unit in which maxInMemorySize is expressed |  | 
|===

[[ErrorMapping]]
=== Error Mapping

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Source a| Enumeration, one of:

** ANY
** REDELIVERY_EXHAUSTED
** TRANSFORMATION
** EXPRESSION
** SECURITY
** CLIENT_SECURITY
** SERVER_SECURITY
** ROUTING
** CONNECTIVITY
** RETRY_EXHAUSTED
** TIMEOUT |  |  | 
| Target a| String |  |  | x
|===

[[LlmResponseAttributes]]
=== LLM Response Attributes

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Additional Attributes a| <<AdditionalAttributes>> |  |  | 
| Token Usage a| <<TokenUsage>> |  |  | 
|===

[[AdditionalAttributes]]
=== Additional Attributes

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Finish Reason a| String |  |  | 
| Id a| String |  |  | 
| Model a| String |  |  | 
|===

[[TokenUsage]]
=== Token Usage

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Input Count a| Number |  |  | 
| Output Count a| Number |  |  | 
| Total Count a| Number |  |  | 
|===

[[Proxy]]
=== Proxy

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Host a| String |  |  | x
| Port a| Number |  |  | x
| Username a| String |  |  | 
| Password a| String |  |  | 
| Non Proxy Hosts a| String |  |  | 
|===

[[NtlmProxy]]
=== Ntlm Proxy

[%header,cols="20s,25a,30a,15a,10a"]
|===
| Field | Type | Description | Default Value | Required
| Ntlm Domain a| String |  |  | x
| Host a| String |  |  | x
| Port a| Number |  |  | x
| Username a| String |  |  | 
| Password a| String |  |  | 
| Non Proxy Hosts a| String |  |  | 
|===
